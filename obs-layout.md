# 방송 화면 배치 (1920 × 1080)

보드만 크게 두고, 이름·시계·채팅을 오른쪽에 따로 얹는 구성입니다.
`obs-overlay/layout-guide.png` 를 자 삼아 맞추면 됩니다.

## 자리표

| 무엇 | 크기 | 위치 |
| --- | --- | --- |
| 배경 | 1920 × 1080 | 0, 0 |
| 보드 | 960 × 960 | x 60, y 60 |
| 상대 이름 | 500 × 70 | x 1080, y 70 |
| 상대 시계 | 220 × 56 | x 1610, y 70 |
| 채팅·캠·정보 | 780 × 400 | x 1080, y 300 |
| 내 이름 | 500 × 70 | x 1080, y 950 |
| 내 시계 | 220 × 56 | x 1610, y 950 |

보드를 화면 높이의 89%까지 키운 배치입니다.
참고한 유튜브 화면은 오른쪽에 채팅을 크게 넣느라 보드가 80%(866px)였는데,
오른쪽에 이름과 시계만 얹을 거라면 이렇게 키우는 편이 보기 좋습니다.
더 키우고 싶으면 보드를 `1000 × 1000 @ x40 y40` 까지 올릴 수 있습니다.

배경색은 **`1F251A`** 입니다. 회색이 아니라 어두운 초록입니다.
이 색이 맞아야 보드 주변 여백이 잘린 화면처럼 보이지 않고 한 화면처럼 읽힙니다.

## 순서

### 1. 캔버스 확인
설정 → 비디오 → **기본 캔버스 해상도** 가 `1920x1080` 인지 봅니다. 다르면 아래 숫자도 비례해 달라집니다.

### 2. 배경 깔기
소스 `+` → **색상 소스** → 이름 `배경` → 색 `21211E` → 확인.
`Ctrl+F` 로 화면 전체에 맞추고, 소스 목록에서 **맨 아래**로 내립니다.

### 3. 정렬 가이드 얹기
소스 `+` → **이미지** → `layout-guide.png` 선택 → `Ctrl+F`.
목록에서 **맨 위**에 둡니다. 배치가 끝나면 눈 아이콘으로 꺼두면 되고, 지울 필요는 없습니다.

### 4. 보드 자르기
1. 체스닷컴 캡처 소스를 우클릭 → **필터** → `+` → **자르기/채우기**
2. 위·아래·좌·우 값을 올려 **보드만** 남깁니다. 이름 줄과 사이드바가 모두 잘려나가게
3. 남은 그림이 정사각형에 가까워야 합니다

### 5. 보드 크기와 자리 맞추기 — 실제로 쓰는 값

1920×1080 창을 통째로 캡처한 소스 기준으로, 아래 값이 검증된 조합입니다.

| 칸 | 값 |
| --- | --- |
| 자르기 왼쪽 / 오른쪽 | `397` / `718` |
| 자르기 위 / 아래 | `148` / `67` |
| 크기 | `2290` × `1288` |
| 위치 | `60`, `43` |

**중요 — `크기` 칸은 잘라낸 영역이 아니라 소스 전체(1920×1080)의 크기입니다.**
그래서 두 값은 항상 16:9 비율을 지켜야 합니다. 여기에 960/960 같은 정사각형 값을 넣으면
소스 전체가 눌리면서 보드가 세로로 길쭉해집니다.

계산은 이렇습니다. 잘라낸 보드 폭이 805px 이고 화면에 960px 로 띄우고 싶다면
배율은 `960 ÷ 805 = 1.1925`, 따라서 크기는 `1920 × 1.1925 = 2290`, `1080 × 1.1925 = 1288`.
보드를 더 키우려면 원하는 크기를 805로 나눠 같은 방식으로 곱하면 됩니다.

위치 Y가 60이 아니라 43인 이유는, 자르기가 보드 위쪽 배경을 14px 정도 함께 남기기 때문입니다.
그만큼(화면에서 약 17px) 올려주면 보드의 윗변이 정확히 60에 옵니다.

**`Ctrl+F`(화면에 맞추기)는 쓰지 마세요.** 소스를 16:9로 강제로 늘려 배율을 깨뜨립니다.
크기는 항상 이 창에서 숫자로 넣으세요.

### 참고 — 자르기를 직접 잡을 때
캡처 소스를 우클릭 → **변환** → **변환 편집**(`Ctrl+E`).

**순서를 지켜야 합니다.**

1. **자르기** 네 칸(왼쪽·오른쪽·위·아래)을 조정해 보드만 남깁니다.
   자르기를 고치면 위쪽 **크기** 숫자가 따라 움직이는데,
   **크기의 두 값이 같아지면** 정사각형이 된 것입니다. 거기까지 맞춥니다.
2. **크기** 칸에 `960`, `960` 을 넣습니다.
3. **위치** 칸에 `60`, `60` 을 넣습니다.
4. 경계 상자 유형은 **경계 없음** 그대로 두고, "경계 상자로 자르기" 도 체크하지 않습니다.

크기의 두 값이 다른 채로 숫자를 넣으면 보드가 찌그러집니다. 1번을 먼저 끝내세요.

### 6. 이름과 시계 얹기
이름은 텍스트 소스로 만듭니다. 소스 `+` → **텍스트(GDI+)** → **파일에서 읽기** 체크 →
`상대.txt` 지정. 방송 중에는 메모장에서 그 파일을 고쳐 저장하면 바로 바뀝니다.

시계는 보드 캡처와 별도로, 원래 화면의 시계 부분만 잘라 쓰는 방법이 있습니다.

1. 체스닷컴 캡처 소스를 우클릭 → **복제** (같은 창을 하나 더 씁니다)
2. 복제본에 **자르기/채우기** 필터를 걸어 시계 숫자만 남깁니다
3. `Ctrl+E` 로 위치를 x `1600`, y `110` 에 놓습니다

상대 시계와 내 시계 두 개가 필요하니 복제를 두 번 합니다.

### 7. 마무리
가이드 이미지를 끄고 미리보기를 봅니다. 여백이 허전하면 채팅·정보 자리에
채팅 위젯이나 캠, 로고를 넣으면 됩니다.

## 해상도가 다를 때

캔버스가 1920×1080이 아니면 위 숫자에 배율을 곱하면 됩니다.
예를 들어 2560×1440 이면 모든 값에 `1.333` 을 곱합니다. (보드 1152, x 155, y 144)
