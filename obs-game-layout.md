# 게임 화면 배치 (헤더 없는 구성)

카드 헤더 없이, 반투명 판 위에 이름과 시계만 올리고 가운데 오른쪽에 채팅을 두는 구성입니다.
참고한 방송 화면을 픽셀 단위로 재서 뽑은 값입니다.

가이드 그림: `obs-overlay/layout-guide-game.png`

## 자리표 (1920 × 1080)

| 무엇 | 크기 | 위치 | 어떻게 |
| --- | --- | --- | --- |
| 배경 | 1920 × 1080 | 0, 0 | 색상 소스 `A7BBB0` |
| 보드 | 866 × 864 | 117, 107 | 캡처 자르기 |
| 상대 이름 | 500 × 60 | 1070, 108 | `plate-name.png` + 텍스트 |
| 상대 시계 | 220 × 60 | 1610, 108 | `plate-clock.png` + 캡처 자르기 |
| 채팅 | 760 × 340 | 1070, 345 | 채팅 위젯 브라우저 소스 |
| 문구 | 760 × 50 | 1070, 780 | `plate-notice.png` + 텍스트 (훈수 금지 등) |
| 내 이름 | 500 × 60 | 1070, 912 | `plate-name.png` + 텍스트 |
| 내 시계 | 220 × 60 | 1610, 912 | `plate-clock.png` + 캡처 자르기 |

보드는 화면 폭의 45%, 높이의 80%입니다. 오른쪽 열은 `x 1070` 에서 시작해 `x 1830` 에서 끝납니다.

## 판과 글자

판은 알파가 들어간 PNG 라 **이미지** 소스로 넣으면 그대로 반투명입니다.

| 파일 | 크기 |
| --- | --- |
| `plate-name.png` | 500 × 60 |
| `plate-clock.png` | 220 × 60 |
| `plate-notice.png` | 760 × 50 |

이름 글자는 **텍스트(GDI+)** 에 **파일에서 읽기** 로 `상대.txt` 를 걸어두면
방송 중 메모장으로 고쳐 저장하는 즉시 바뀝니다. 글자색은 `E4F0ED`.

판 위에 글자를 가운데 놓으려면, 텍스트 소스의 정렬을 가운데로 두고
`Ctrl+E` 에서 판과 같은 위치·크기를 주는 게 가장 정확합니다.

## 쌓는 순서

위에서부터

    이름 글자 / 문구 글자
    시계 (캡처 자른 것)
    판 (plate-*.png)
    채팅
    보드 (캡처 자른 것)
    배경

## 시계

시계는 체스닷컴 캡처를 참조로 복제해 시계 부분만 자른 것입니다.
**체스닷컴은 차례인 쪽 시계를 밝게 뒤집습니다.** 배경을 지우려 하지 말고 상자째 쓰고,
뒤에 판을 8px 씩 크게 깔아 액자처럼 감싸면 자연스럽습니다.

- 시계: `220 × 60 @ 1610, 108`
- 판: `236 × 76 @ 1602, 100`

## 이름 칸이 어색할 때

각진 판에 글자가 꽉 차면 이름표를 억지로 붙인 것처럼 보입니다. 네 가지 방법이 있습니다.

**1. 판을 아예 빼기 (가장 깔끔)**
배경이 밝으니 글자만 어두운 색(`16282A`)으로 올려도 잘 읽힙니다.
참고한 방송 화면도 이름 뒤에 판이 없습니다.

**2. 알약 모양 반투명 판**
모서리를 완전히 둥글게(반지름 = 높이의 절반) 깎고 투명도를 더 올린 판입니다.
`plate-name-pill.png` (500 × 60), `plate-clock-pill.png` (260 × 60) 가 그것입니다.
불투명도 37% 라 배경이 비쳐서 판이 얹힌 티가 덜 납니다.

**3. 아주 옅은 판 + 밑줄**
판은 거의 안 보일 만큼 옅게 두고, 아래에 `2F5A57` 밑줄을 3px 깔아 자리만 표시합니다.

**4. 판 없이 글자에 그림자**
텍스트 소스 속성에서 **그림자** 를 켜고 색을 검정 40% 정도로 둡니다.
판 없이도 글자가 배경에서 떠 보입니다.

### 글자 자체

- **판보다 글자가 작아야 합니다.** 판 높이가 60이면 글자는 28~32px 정도. 지금처럼 글자가 판을
  꽉 채우면 답답해 보입니다.
- 좌우 여백도 넉넉히. 글자 양옆으로 최소 20px 은 비워두세요.
- 기본 글꼴(굴림, 맑은 고딕)은 방송에서 투박해 보입니다. 무료 한글 글꼴 중
  **프리텐다드(Pretendard)**, **나눔스퀘어**, **에스코어드림** 이 화면에 깔끔합니다.
  설치하면 OBS 텍스트 소스의 글꼴 목록에 바로 나옵니다.
- 굵기는 Bold 보다 **SemiBold/Medium** 이 이름에는 잘 맞습니다.
