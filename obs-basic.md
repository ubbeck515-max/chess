# OBS 기본 소스만으로 가리기

확장도, 오버레이 파일도 쓰지 않습니다. OBS에 들어 있는 **색상 소스**와 **텍스트 소스**
두 가지로 끝냅니다. 글자는 메모장으로 바꾸면 방송에 바로 반영됩니다.

## 0. 준비 — 글자 파일 만들기

바꿀 글자마다 텍스트 파일을 하나씩 만듭니다. 위치는 아무 데나, 찾기 쉬운 곳이면 됩니다.
예를 들어 `문서\방송\` 폴더에

- `상대.txt` — 내용: `상대`
- `나.txt` — 내용: `나`

메모장으로 만들 때 **다른 이름으로 저장 → 인코딩을 `UTF-8`** 로 두세요.
한글이 깨지면 십중팔구 이 인코딩 때문입니다.

## 1. 가림 박스 만들기

1. OBS 아래 **소스** 패널에서 `+` → **색상 소스**
2. 이름을 `가림-상대이름` 으로 하고 확인
3. 색을 고릅니다. 체스닷컴 어두운 배경과 맞추려면 `262421`,
   그냥 까맣게 덮으려면 검정도 괜찮습니다. 너비·높이는 아무렇게나 두고 확인
4. 미리보기 화면에서 그 빨간 테두리 사각형을 **끌어서 이름 위에 올리고**,
   모서리를 잡아 크기를 맞춥니다

이름 자리마다 3~4번을 반복합니다. 게임 화면이라면 보드 위쪽 상대 이름, 아래쪽 내 이름
두 개면 충분합니다.

## 2. 글자 얹기

1. `+` → **텍스트(GDI+)**
2. 이름을 `라벨-상대` 로 하고 확인
3. 속성 창에서 **파일에서 읽기** 를 체크
4. **찾아보기** 를 눌러 `상대.txt` 를 고릅니다
5. 글꼴 크기와 색을 정하고 확인
6. 미리보기에서 글자를 가림 박스 위로 끌어다 놓습니다

**소스 목록에서 텍스트가 색상 소스보다 위에 있어야** 글자가 보입니다.
목록은 위에 있는 것이 앞에 그려집니다.

## 3. 방송 중에 글자 바꾸기

메모장으로 `상대.txt` 를 열어 고치고 **저장(Ctrl+S)** 하면 끝입니다.
OBS를 건드릴 필요도, 소스를 새로 고칠 필요도 없습니다. 저장하는 순간 화면이 바뀝니다.

메모장을 아예 띄워둔 채로 방송하면, 상대가 바뀔 때마다 한 줄 고치고 저장만 하면 됩니다.

## 4. 묶어서 단축키로 켜고 끄기

박스가 여러 개면 하나씩 껐다 켜기 번거로우니 묶습니다.

1. 소스 목록에서 관련된 것들을 `Ctrl` 로 여러 개 선택
2. 우클릭 → **선택한 항목 그룹화**
3. 그룹 이름을 `가림-게임` 처럼 정합니다
4. 설정 → **단축키** 에서 그 그룹의 **표시 / 숨기기** 에 키를 지정합니다

화면마다 그룹을 하나씩 만들어 두면 `F1`, `F2` 로 갈아끼울 수 있습니다.

## 5. 목록 화면은 잘라내기

경기 기록이나 친구 목록처럼 스크롤하면 이름이 움직이는 화면은 박스로 쫓아다닐 수 없습니다.
그 부분을 아예 화면 밖으로 빼는 편이 확실합니다.

1. 캡처 소스를 우클릭 → **필터**
2. 왼쪽 아래 `+` → **자르기/채우기**
3. 위·아래·좌·우 값을 올려 이름이 몰린 영역을 잘라냅니다

보드만 남기고 자르면 홈 화면에 있어도 목록이 화면에 안 잡힙니다.

## 이 방식의 한계

- 박스 위치는 고정입니다. 브라우저 창 크기를 바꾸면 체스닷컴 레이아웃이 달라져 어긋납니다.
  **방송할 때 쓰는 창 크기를 정해두고 바꾸지 마세요.**
- 보드의 이름과 레이팅은 `이름 (2081)` 처럼 붙어 있고 레이팅 위치가 이름 길이에 따라 움직입니다.
  레이팅까지 가려도 괜찮다면 전체를 넉넉히 덮는 편이 안전합니다.
- 매칭되는 순간 중앙에 뜨는 이름은, 중앙을 덮는 색상 소스를 하나 만들어 두고
  게임 시작을 누르기 직전에 단축키로 켰다가 보드가 나오면 끄면 됩니다.
