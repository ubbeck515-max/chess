# 패널형 방송 화면 만들기

보드·기보·메모장·채팅을 각각 제목 달린 카드에 담는 구성입니다.
색상 소스와 텍스트 소스만으로 만들 수 있고, 특별한 프로그램은 필요 없습니다.

가이드 그림: `obs-overlay/layout-guide-panel.png`

## 색

| 쓰임 | 값 |
| --- | --- |
| 배경 | `2B2935` |
| 제목 바 / 카드 헤더 | `42325A` |
| 카드 본문 | `22212A` |
| 테두리 | `9682C8` |

## 자리표 (1920 × 1080)

| 무엇 | 크기 | 위치 |
| --- | --- | --- |
| 상단 제목 바 | 1920 × 70 | 0, 0 |
| 보드 카드 | 790 × 890 | 80, 110 |
| 기보·분석 카드 | 420 × 890 | 950, 110 |
| 메모장 카드 | 440 × 420 | 1420, 110 |
| 채팅 카드 | 440 × 420 | 1420, 580 |

카드 헤더는 모두 높이 `46` 입니다. 본문은 그 아래부터 시작합니다.
예를 들어 보드 카드의 본문은 `790 × 844 @ 80, 156` 입니다.

## 1. 배경 깔기

1. 소스 `+` → **색상 소스** → 이름 `배경`
2. 색 칸을 눌러 16진수에 `2B2935` 입력 → 확인
3. 너비 `1920`, 높이 `1080` → 확인
4. 소스 목록에서 **맨 아래**로 내립니다

색상 소스는 만들 때 크기를 정하지만, 나중에 `Ctrl+E` 에서 위치·크기를 숫자로 바꿔도 됩니다.
색상 소스는 단색이라 `Ctrl+F` 로 늘려도 찌그러질 게 없습니다. 캡처 소스와 달리 안전합니다.

## 2. 카드 만들기

카드 하나는 **색상 소스 두 개 + 텍스트 소스 하나**입니다.

1. **본문** — 색상 소스, 색 `22212A`, 크기와 위치는 위 표대로
2. **헤더** — 색상 소스, 색 `42325A`, 같은 x·y 에 높이만 `46`
3. **제목** — 텍스트(GDI+), 글자를 적고 헤더 위에 올림

테두리를 넣고 싶으면 본문보다 2px 씩 큰 색상 소스(`9682C8`)를 하나 더 깔고
그 위에 본문을 얹으면 테두리처럼 보입니다.

만든 카드의 소스들을 `Ctrl` 로 함께 선택 → 우클릭 → **선택한 항목 그룹화** 해두면
나중에 통째로 옮기기 편합니다.

## 3. 카드 안을 채우기

- **보드** — 체스닷컴 캡처를 자르기로 보드만 남겨 본문 위에 올립니다.
  (`obs-layout.md` 의 자르기·크기 값 참고)
- **기보·분석** — 같은 캡처 소스를 **복제**해서, 자르기로 기보 패널만 남깁니다.
  창 하나를 두 군데에 나눠 쓰는 셈입니다.
- **메모장** — 텍스트(GDI+) 에 **파일에서 읽기** 로 `메모.txt` 를 지정합니다.
  방송 중 메모장에서 고치고 저장하면 화면이 바로 바뀝니다.
- **채팅** — 치지직·유튜브의 채팅 위젯 주소를 **브라우저 소스**로 넣습니다.

## 4. 순서 정리

소스 목록의 위아래가 그리는 순서입니다. 위에 있는 것이 앞에 나옵니다.

    제목 텍스트
    캡처 / 채팅 / 메모
    카드 헤더
    카드 본문
    배경

이 순서가 뒤집히면 배경이 내용을 덮어버립니다. 뭔가 안 보이면 여기부터 확인하세요.
