# 패널형 방송 화면 만들기

보드·기보·메모장·채팅을 각각 제목 달린 카드에 담는 구성입니다.
색상 소스와 텍스트 소스만으로 만들 수 있고, 특별한 프로그램은 필요 없습니다.

가이드 그림: `obs-overlay/layout-guide-panel.png`

## 색

| 쓰임 | 값 |
| --- | --- |
| 배경 | `9BBBB6` |
| 제목 바 / 카드 헤더 | `2F5A57` |
| 카드 본문 | `1C2A2C` |
| 테두리 | `6E9B96` |
| 헤더 글자 | `E4F0ED` |
| 배경 위 글자 | `16282A` |

밝은 청록 배경 위에 어두운 카드를 얹는 구성입니다.
배경과 카드가 같은 계열이라 통일감이 있으면서, 명도 차이가 커서 경계는 뚜렷합니다.
**배경이 밝으므로 배경 위에 얹는 글자는 어두운 색(`16282A`)을 쓰세요.** 흰 글자는 보이지 않습니다.

강조색이 필요하면(알림, 버튼 등) `D4813F` 주황이 잘 맞습니다. 청록의 보색이라 한 군데만 써도 살아납니다.

## 자리표 (1920 × 1080)

| 무엇 | 크기 | 위치 |
| --- | --- | --- |
| 상단 제목 바 | 1920 × 70 | 0, 0 |
| 보드 카드 | 790 × 890 | 80, 110 |
| 기보·분석 카드 | 420 × 890 | 950, 110 |
| 메모장 카드 | 440 × 420 | 1420, 110 |
| 채팅 카드 | 440 × 420 | 1420, 580 |

카드 헤더는 모두 높이 `46` 입니다. 본문은 그 아래부터 시작합니다.
예를 들어 보드 카드의 본문은 `790 × 844 @ 80, 156` 입니다.

## 1. 배경 깔기

1. 소스 `+` → **색상 소스** → 이름 `배경`
2. 색 칸을 눌러 16진수에 `2B2935` 입력 → 확인
3. 너비 `1920`, 높이 `1080` → 확인
4. 소스 목록에서 **맨 아래**로 내립니다

색상 소스는 만들 때 크기를 정하지만, 나중에 `Ctrl+E` 에서 위치·크기를 숫자로 바꿔도 됩니다.
색상 소스는 단색이라 `Ctrl+F` 로 늘려도 찌그러질 게 없습니다. 캡처 소스와 달리 안전합니다.

## 2. 카드 만들기

카드 하나는 **색상 소스 두 개 + 텍스트 소스 하나**입니다.

1. **본문** — 색상 소스, 색 `22212A`, 크기와 위치는 위 표대로
2. **헤더** — 색상 소스, 색 `42325A`, 같은 x·y 에 높이만 `46`
3. **제목** — 텍스트(GDI+), 글자를 적고 헤더 위에 올림

테두리를 넣고 싶으면 본문보다 2px 씩 큰 색상 소스(`9682C8`)를 하나 더 깔고
그 위에 본문을 얹으면 테두리처럼 보입니다.

### 소스 묶기 (그룹)

카드 하나가 소스 서너 개라 금방 목록이 길어집니다. 묶어두면 통째로 다룰 수 있습니다.

1. 소스 목록에서 첫 소스를 클릭하고, **`Ctrl` 을 누른 채** 나머지를 클릭해 여러 개를 고릅니다
2. 그 위에서 **우클릭 → 선택한 항목 그룹화**
3. 이름을 정합니다 (`카드-기보`, `카드-메모` 처럼)

그룹 왼쪽의 화살표로 접었다 펼 수 있고, 눈 아이콘 하나로 그룹 전체를 켜고 끕니다.
설정 → 단축키에서 **그룹의 표시/숨기기**에 키를 걸면 화면 전환도 한 번에 됩니다.

알아둘 점 몇 가지.

- 그룹을 선택하고 `Ctrl+E` 로 크기를 바꾸면 **안에 든 소스가 모두 같이** 커지거나 작아집니다.
  카드 하나만 조절하려면 그룹을 펼쳐 그 소스를 직접 고르세요.
- 이미 있는 소스를 그룹에 넣으려면, 그룹을 펼친 상태에서 소스를 **끌어다 그룹 안으로** 놓습니다.
- 묶은 걸 풀려면 그룹에서 우클릭 → **그룹 해제**. 안의 소스는 그대로 남습니다.
- 그룹 이름도 소스 목록에 하나의 줄로 잡히므로, 그룹끼리의 앞뒤 순서도 그리는 순서에 영향을 줍니다.

## 3. 카드 안을 채우기

- **보드** — 체스닷컴 캡처를 자르기로 보드만 남겨 본문 위에 올립니다.
  (`obs-layout.md` 의 자르기·크기 값 참고)
- **기보·분석** — 같은 캡처 소스를 **복제**해서, 자르기로 기보 패널만 남깁니다.
  창 하나를 두 군데에 나눠 쓰는 셈입니다.
- **메모장** — 텍스트(GDI+) 에 **파일에서 읽기** 로 `메모.txt` 를 지정합니다.
  방송 중 메모장에서 고치고 저장하면 화면이 바로 바뀝니다.
- **채팅** — 치지직·유튜브의 채팅 위젯 주소를 **브라우저 소스**로 넣습니다.

## 4. 순서 정리

소스 목록의 위아래가 그리는 순서입니다. 위에 있는 것이 앞에 나옵니다.

    제목 텍스트
    캡처 / 채팅 / 메모
    카드 헤더
    카드 본문
    배경

이 순서가 뒤집히면 배경이 내용을 덮어버립니다. 뭔가 안 보이면 여기부터 확인하세요.

## 대기 화면 만들기

경기 기록을 뒤지거나 잠시 자리를 비울 때 넘어갈 화면입니다. 코드는 필요 없고,
**장면(Scene)** 을 하나 더 만들어 채우면 됩니다.

1. 왼쪽 아래 **장면** 패널에서 `+` → 이름 `대기`
2. 그 장면에 소스 `+` → **이미지** → `obs-overlay/standby-bg.png` → `Ctrl+F`
3. 소스 `+` → **텍스트(GDI+)** 로 문구를 얹습니다 (`잠시 후 돌아옵니다` 등)
   배경 그림의 밑줄 두 개가 글자 얹을 자리입니다. 글자색은 `E4F0ED`
4. 필요하면 음악이나 캠도 이 장면에만 추가합니다

`standby-bg.png` 는 같은 팔레트(배경 `9BBBB6`, 카드 `1C2A2C`)로 만든 1920×1080 그림이라
본 화면과 이어져 보입니다. 체스판 무늬는 배경보다 한 톤 어두운 정도라 은은합니다.

## 단축키

설정 → **단축키** 에서 지정합니다. 목록이 길지만 항목 이름으로 찾으면 됩니다.

| 무엇 | 항목 | 추천 키 |
| --- | --- | --- |
| 본 화면으로 | `게임` 장면의 **장면 전환** | `F1` |
| 대기 화면으로 | `대기` 장면의 **장면 전환** | `F2` |
| 매칭 가림 | 그 소스/그룹의 **표시**와 **숨기기**에 같은 키 | `F5` |

장면 전환은 항목이 하나뿐이라 키를 한 번만 넣으면 되고,
소스나 그룹은 **표시**와 **숨기기**가 따로 있습니다. 같은 키를 둘 다에 넣으면 토글이 됩니다.

게임 중에 실수로 누르지 않도록, 체스를 두면서 쓸 일 없는 키(`F1`~`F5`, `Ctrl+Alt+숫자`)를
고르는 편이 안전합니다.
